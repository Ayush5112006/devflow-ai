# GridGuard AI — Frontend Environment Variables
# Copy this file to .env and fill in values when backend is ready.

# Backend API base URL (leave as-is until FastAPI is running)
VITE_API_BASE_URL=/api
